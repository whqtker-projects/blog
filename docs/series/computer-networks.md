# Computer Networks

## Purpose

`computer-networks` groups the current networking backlog into coherent child series so protocol, transport, and naming/routing topics do not collapse into one mixed list.

## Child Series Composition

| Order | Child series | Current posture |
|---|---|---|
| 1 | `network-protocols` | 1 draft, 2 idea |
| 2 | `transport-and-reliability` | 1 draft, 1 idea |
| 3 | `naming-and-routing` | 2 idea |

Current child indexes:
- `src/content/series_indexes/computer-networks/network-protocols.md`
- `src/content/series_indexes/computer-networks/transport-and-reliability.md`
- `src/content/series_indexes/computer-networks/naming-and-routing.md`

## Backlog Posture

This parent is mid-rollout:
- the former flat `network-protocols` backlog has already been split into three child series
- explicit child ordering is in place
- post statuses are still mostly `idea` or `draft`, so this parent remains backlog-heavy rather than publication-complete

## Next Expansion Points

- Promote the existing draft/idea posts instead of adding broad new child series too early.
- Keep transport and naming/routing topics in their current sibling series rather than drifting back into `network-protocols`.
- When a new networking slice is truly distinct, add it as another child series under `computer-networks` with its own `order`.
